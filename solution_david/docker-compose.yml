services:
  js-api:
    build:
      context: ./apis/js
      dockerfile: Dockerfile
    ports:
      - "3000:3000"

  python-api:
    build:
      context: ./apis/python
      dockerfile: Dockerfile
    ports:
      - "4000:4000"

  go-api:
    build:
      context: ./apis/go
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

