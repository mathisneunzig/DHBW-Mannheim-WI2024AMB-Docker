services:
  go:
    build:
      context: ./go
      dockerfile: Dockerfile
    ports:
      - "5001:5000"

  js:
    build:
      context: ./js
      dockerfile: Dockerfile
    ports:
      - "3000:3000"

  python:
    build:
      context: ./python
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
